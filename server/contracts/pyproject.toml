[project]
name = "cheat-eraser-contracts"
version = "0.1.0"
description = "Shared models for Cheat Eraser services"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.13.5",
]

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
